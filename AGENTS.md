# Repository Guidelines

## Tool Documentation

When adding, removing, renaming, or materially changing an exported tool, update
`README.md` in the same change. Treat `src/entry.tools-ts.ts` as the source of
truth for what is exported, and keep the README's high-level tools overview
aligned with it so future agents can quickly choose the right utility.
